---
interface Props {
  text?: string;
}

const {
  text = "Nuestra misión es transformar la cultura organizacional de los establecimientos comerciales, integrando procesos de seguridad, salud y medio ambiente que impulsen la productividad y aseguren el bienestar de todos los colaboradores.",
} = Astro.props;
---

<section
  class="py-16 sm:py-24 bg-white border-y border-slate-100"
  aria-labelledby="purpose-heading"
>
  <div class="max-w-4xl mx-auto px-4 sm:px-6 text-center" data-animate>
    <div
      class="inline-block p-3 rounded-2xl bg-primary/10 mb-6"
      aria-hidden="true"
    >
      <span class="material-icons text-primary text-4xl">verified_user</span>
    </div>
    <h2
      id="purpose-heading"
      class="text-2xl sm:text-3xl md:text-4xl font-extrabold mb-6 sm:mb-8 text-hseq-dark"
    >
      Nuestro Propósito
    </h2>
    <blockquote
      class="text-lg sm:text-xl md:text-2xl text-slate-600 leading-relaxed italic"
    >
      <p>{text}</p>
    </blockquote>
  </div>
</section>
